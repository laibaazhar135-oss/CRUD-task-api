services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: devpassword123
      POSTGRES_DB: tasksdb
    ports:
      - "5432:5432"
    volumes:
      - tasks-pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:devpassword123@db:5432/tasksdb
      PORT: 3000
    depends_on:
      - db

volumes:
  tasks-pgdata: